# Ollama server endpoint (used by langchain-ollama for embeddings + LLM)
OLLAMA_BASE_URL=http://localhost:11434

# Google AI Studio API key (used when switching to gemini-2.0-flash / gemini-2.5-flash-lite)
GEMINI_API_KEY=
