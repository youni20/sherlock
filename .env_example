# Google AI Studio API key — get one at https://aistudio.google.com
GEMINI_API_KEY=

# Ollama endpoint — only needed when using local Ollama models
# Inside Docker (with docker compose): http://host.docker.internal:11434
# Outside Docker (local dev): http://localhost:11434
OLLAMA_BASE_URL=http://host.docker.internal:11434
